package apache.zap.spark.conf

//import pure

object PureConfigReaders {
  // readers

//  implicit lazy val settingsReader: ConfigReader[Settings] =
//    deriveReader[Settings]

  //  def productHintWithInputFilesAndExtraOverrides[A](extraOverrides: (String, String)*): ProductHint[A] = {
//    val overrides = extraOverrides
//    ProductHint[A](ConfigFieldMapping(CamelCase, KebabCase).withOverrides(overrides: _*))
//  }

}
